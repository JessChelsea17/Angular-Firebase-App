export const userConfig = {
    collection_endpoint: 'user'
};

 export const tasksConfig = {
    collection_endpoint: 'tasks'
 };

 export const adminConfig = {
   collection_endpoint: 'profile'
};

export const postConfig = {
   collection_endpoint: 'posts'
};