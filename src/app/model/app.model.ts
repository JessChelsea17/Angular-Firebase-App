export class userProfile {
    name:string;
    email:string;
}

export interface Task {
    id: string;
    name: string;
}

export interface User {
    id: string;
    firstName: string;
    lastName: string;
    birthday: string
    age: string;
    gender: string;
}